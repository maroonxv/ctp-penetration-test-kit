# --- 交易测试配置 ---
# 测试目标合约代码
test_symbol: "IO2602-C-4750"

# 略微低于最新价的价格
safe_buy_price: 70

# 略微高于最新价的价格
deal_buy_price: 80

# --- 风控阈值配置 ---
# 用于模拟和触发合规性预警的限制条件
risk_thresholds:
  # 单个会话允许的最大累计报单笔数 (触发 2.3.1.1 预警)
  max_order_count: 5
  
  # 单个会话允许的最大累计撤单笔数 (触发 2.3.1.3 预警)
  max_cancel_count: 5
  
  # 相同指令(合约、价格、方向等完全一致)允许的最大重复次数 (触发重复报单监测 2.2.3)
  max_symbol_order_count: 3

rest_test_symbol: "LC2607"

rest_test_price: 168220
